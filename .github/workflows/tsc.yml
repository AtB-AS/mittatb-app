name: TypeScript Check, Lint & Test
on:
  workflow_dispatch:
  push:
    paths:
      - '**.js'
      - '**.ts'
      - '**.tsx'
      - '**/tsc.yml'
      - '**.lock'
      - 'package.json'
      - 'patches/**'

jobs:
  tsc:
    name: tsc
    strategy:
      matrix:
        org: [atb, nfk, fram, troms]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: pnpm/action-setup@v4
      - name: install node
        uses: actions/setup-node@v1
        with:
          node-version-file: '.nvmrc'
      - name: Add Entur private registry credentials
        run: sh ./scripts/add-entur-private-registry.sh
        env:
          ENTUR_REGISTRY_USER: ${{ vars.ENTUR_REGISTRY_USER }}
          ENTUR_REGISTRY_TOKEN: ${{ secrets.ENTUR_REGISTRY_TOKEN }}
      - uses: actions/cache@v3
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-modules-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ hashFiles('**/patches/**.patch') }}
      - name: pnpm install
        run: pnpm install --frozen-lockfile
      - name: Install assets
        run: sh ./scripts/generate-assets.sh ${{ matrix.org }}
      - name: check-all
        run: pnpm check-all
