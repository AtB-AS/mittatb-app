
{
    "name": "@entur/bluetooth-challenge-react-native-lib",
    "version": "1.0.0",
    "description": "React Native wrapper for recieveing challenges over bluetooth broadcast",
    "main": "src/index",
    "source": "src/index",
    "scripts": {
        "lint": "eslint --ext .ts src/",
        "test": "jest  --config ../../jest.config.js --rootDir .",
        "ts": "tsc --noEmit"
    },
    "peerDependencies": {
        "react": "*",
        "react-native": "*"
    }
}

