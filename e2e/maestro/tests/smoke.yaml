appId: ${APP_ID}
name: 'Run smoke tests'
onFlowStart:
  - runFlow:
      file: ../elements/loadElements.yaml
      label: 'Load elements'
  - launchApp:
      clearState: true
      permissions:
        location: deny
onFlowComplete:
  - stopApp
---
# Login
- runFlow:
    file: ../common/onboarding/loginPhone.yaml
    env:
      PHONE_NUMBER: ${PHONE_NUMBER}
      OTP: ${OTP}
      INSPECTION: false
    label: 'Login through onboarding'

# Departures
- runFlow:
    file: departures/searchForStopPlace.yaml
    label: 'Departures'

# Travel search
- runFlow:
    file: travelSearch/simpleTravelSearch.yaml
    label: 'Travel search'

# Tickets
- runFlow:
    file: tickets/ticketPurchase.yaml
    label: 'Ticket purchase'

- runFlow:
    file: ../common/logout.yaml
    label: 'Log out'
