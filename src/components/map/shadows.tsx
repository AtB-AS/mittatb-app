import {ViewStyle} from 'react-native';

const shadows: ViewStyle = {
  shadowColor: '#000000',
  shadowOffset: {
    width: 0,
    height: 2,
  },
  shadowOpacity: 0.25,
  shadowRadius: 8,
  elevation: 8,
};

export default shadows;
